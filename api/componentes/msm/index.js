const sms = require('./controller');

/* sms.then((data) => {
        let dataResult = data.MessageId
        console.log(`Mensaje ID: ${dataResult}`);
    }).catch((err) => {
        console.error(err, err.stack);
    });
     */

module.exports = sms